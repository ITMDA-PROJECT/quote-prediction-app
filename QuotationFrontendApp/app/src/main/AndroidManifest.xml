<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.QuotationFrontendApp"
        android:networkSecurityConfig="@xml/network_security_config">


        <!-- Login screen -->
        <activity
            android:name="com.example.quotationfrontendapp.team1.Login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".team1.UpdateUser" />

        <activity
            android:name=".team1.ResetPassword"
            android:exported="true">
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="myapp"
                    android:host="reset"
                    android:path="/reset-password" />
            </intent-filter>
        </activity>

        <activity
            android:name=".team1.ForgotPassword"
            android:exported="true" />

        <!-- Register screen -->
        <activity
            android:name=".team1.Register"
            android:exported="true" />


        <activity
            android:name=".team2.HomeActivity"
            android:exported="true" />

        <activity
            android:name=".team2.AddQuoteActivity"
            android:exported="true" />

        <activity
            android:name=".team2.AddPartActivity"
            android:exported="true" />

        <activity
            android:name=".team2.ViewQuoteActivity"
            android:exported="true" />

        <activity
            android:name=".team2.EditPartActivity"
            android:exported="true" />


        <activity
            android:name="com.example.quotationfrontendapp.team3.ViewCompletedQuotes"
            android:exported="true" />

        <activity
            android:name=".team3.ViewCurrentQuotes"
            android:exported="false" />



    </application>

</manifest>